/**
 * Hosts the sheets that any tab can open. Filled in feature by feature; each
 * sheet mounts only while it is open so its local state starts fresh.
 */
export function SheetHost() {
  return null
}
